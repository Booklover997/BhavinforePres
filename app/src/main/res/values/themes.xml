<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BhavinForPresident" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item> <!-- Hide the title bar -->
        <item name="android:windowActionBar">false</item> <!-- Hide the action bar -->
        <item name="android:colorPrimary">@color/primary</item> <!-- Set your desired primary color -->
    </style>

    <style name="Theme.BhavinForPresident" parent="Base.Theme.BhavinForPresident" />
</resources>